apiVersion: v2
name: workout-service
description: Helm chart for the FitForge Workout Service
type: application
version: 1.0.0
appVersion: "1.0.0"

dependencies:
  - name: fitforge-library
    version: "1.0.0"
    repository: "file://../../../helm-library"
    # ↑ Adjust this path depending on where this chart lives:
    #   - If inside fitforge-shared alongside helm-library: "file://../../helm-library"
    #   - If inside the workout-service repo with library published to OCI/ChartMuseum,
    #     replace with: repository: "oci://ghcr.io/fitforge101/helm-charts"
